version: '3.1'

services:
  mongo:
    image: mongo
    restart: always
    volumes:
       - /home/ubuntu/mongodb/data:/data/db
    environment:
      MONGO_INITDB_ROOT_USERNAME: degpeg
      MONGO_INITDB_ROOT_PASSWORD: uu42gtK3Y02n
    ports:
      - "27017:27017"
